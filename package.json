{
  "name": "maasolar-app",
  "version": "1.0.0",
  "description": "MAA Solar Quotation Generator",
  "scripts": {
    "install": "cd client && npm install && cd ../server && npm install",
    "build": "cd client && npm run build",
    "start": "cd server && npm start"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
